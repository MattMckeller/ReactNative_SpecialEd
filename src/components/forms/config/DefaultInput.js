import autoFormErrorDisplay from '../hoc/AutoFormErrorDisplay';
import RoundedTextInput from '../../shared/common/RoundedTextInput';

export const DefaultInput = autoFormErrorDisplay(RoundedTextInput);
