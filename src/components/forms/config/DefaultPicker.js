import autoFormErrorDisplay from '../hoc/AutoFormErrorDisplay';
import RoundedPickerInput from '../../shared/common/RoundedPickerInput';

export const DefaultPicker = autoFormErrorDisplay(RoundedPickerInput);
